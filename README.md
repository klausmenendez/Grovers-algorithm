# Grovers-algorithm
Benchmarked simulated execution time and oracle call scaling for Grover’s algorithm versus classical search, demonstrating the expected  O(N^(1/2) ) speedup in query complexity.
